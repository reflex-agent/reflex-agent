// Test stub for the `server-only` package, which throws when imported
// outside a React Server Component. Aliased in vitest.config.ts so kernel
// modules under lib/server/** can be exercised in a plain Node context.
export {};
